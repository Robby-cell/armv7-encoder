[package]
name = "armv7-encoder"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
wasm = ["wasm-bindgen"]

[dependencies]
nom = "8.0"
wasm-bindgen = { version = "0.2", optional = true }
